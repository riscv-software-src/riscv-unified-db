.Extensions
<%- arch_def.extensions.sort { |a, b| a.name <=> b.name }.each do |ext| -%>
* %%LINK[ext;<%= ext.name %>;<%= ext.name %>]%%
<%- end -%>

.Control and Status Registers
<%- arch_def.csrs.sort { |a, b| a.name <=> b.name }.each do |csr| -%>
* %%LINK[csr;<%= csr.name %>;<%= csr.name %>]%%
<%- end -%>

.Instructions
<%- arch_def.instructions.sort { |a, b| a.name <=> b.name }.each do |inst| -%>
* %%LINK[inst;<%= inst.name %>;<%= inst.name %>]%%
<%- end -%>

.IDL functions
* xref:funcs:funcs.adoc[Global function defintions]

.Appendix
* xref:prose:idl.adoc[IDL guide]