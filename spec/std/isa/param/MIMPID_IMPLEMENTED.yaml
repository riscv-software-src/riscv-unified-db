# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: MIMPID_IMPLEMENTED
long_name: Whether or not the `mimpid` CSR is implemented.
definedBy:
  extension:
    name: Sm
description: |
  * false: `mimpid` is not implemented, and must be read-only-0
  * true: `mimpid` is implemented, and the value is determined by `IMP_ID_VALUE`
schema:
  type: boolean
