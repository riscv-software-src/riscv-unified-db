# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: REPORT_VA_IN_MTVAL_ON_INSTRUCTION_MISALIGNED
long_name: |
  Whether or not `mtval` is written with the virtual address of a misaligned fetch
definedBy:
  allOf:
    - extension:
        name: Sm
description: |
  Options:

    * true:  `mtval` is written with the virtual address of a trapping misaligned fetch
    * false: `mtval` is written with 0 when a misaligned fetch traps

schema:
  type: boolean
