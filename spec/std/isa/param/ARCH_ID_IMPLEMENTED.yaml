# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: ARCH_ID_IMPLEMENTED
long_name: Whether or not `marchid` is implemented
definedBy:
  extension:
    name: Sm
description: |
  Whether or not `marchid` is implemented. When false, `marchid` will be read-only-0.

  "This register must be readable in any implementation, but a value of 0 can be returned to indicate the
  field is not implemented.""
schema:
  type: boolean
