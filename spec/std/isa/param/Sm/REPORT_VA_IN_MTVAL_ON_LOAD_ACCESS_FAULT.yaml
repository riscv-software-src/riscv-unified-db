# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: REPORT_VA_IN_MTVAL_ON_LOAD_ACCESS
long_name: |
  Whether or not `mtval` is written with the virtual address of a load causing an access fault
definedBy:
  allOf:
    - extension:
        name: Sm
description: |
  Options:

    * true:  `mtval` is written with the virtual address of a load causing the access fault
    * false: `mtval` is written with 0 when a load causes an access fault

schema:
  type: boolean
