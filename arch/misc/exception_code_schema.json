{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "file://arch/misc/exception_code_schema.json",


  "type": "object",
  "$description": "Dictionary of exception code number to name",
  "propertyNames": {
    "pattern": "^[0-9]+$",
    "$description": "Exception number, as a string (JSON doesn't have number object keys). Note that in YAML, the keys may be integers!"
  }
}
